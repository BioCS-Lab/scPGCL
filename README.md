## Dependencies
# In the project root directory
conda env create -f environment.yml

# Activate the environment
conda activate scpgcl


## Datasets
The project uses multiple single-cell proteomics datasets stored in the raw_data folder:
·N2.h5ad
·nanoPOTS.h5ad
·plexDIA.h5ad
·pSCoPE_Huffman.h5ad
·pSCoPE_Leduc.h5ad
·SCoPE2_Leduc.h5ad
These datasets are preprocessed using scPGCL-main/create_data.py to generate the following derived datasets:
| Dataset                   |  Source Files                        |  
| --------------------------| --------------------------------------|
| SCoPE2_Specht             | SCoPE2_Specht                         | 
| 1_nanoPOTS&N2             | nanoPOTS.h5ad, N2.h5ad                |
| 2_SCoPE2_Leduc&plexDIA    | SCoPE2_Leduc.h5ad, plexDIA.h5ad       | 
| 3_pSCoPE_Huffman_plexDIA  | pSCoPE_Huffman.h5ad, plexDIA.h5ad     |  
| 4_pSCoPE_Leduc&plexDIA    | pSCoPE_Leduc.h5ad, plexDIA.h5ad       | 
|5_SCoPE2_Leduc&pSCoPE_Leduc| SCoPE2_Leduc.h5ad, pSCoPE_Leduc.h5ad  |

Note: Each dataset is generated by running create_data.py with the corresponding raw data files as input.


## Graph Construction
To prepare the dataset for downstream graph-based learning, we construct a graph from the integrated single-cell 
proteomic data stored in an .h5ad file.
We provide a script create_graph.py to convert the .h5ad data into a graph structure.
The generated graph is saved as a PyTorch file (.pt) in:
ReGCL-main/dataset_bag/

## Data Preprocessing
After constructing the graph data, we further preprocess it using the provided script data_process.py to prepare 
it for downstream training.

## Training
After preprocessing, the data can be directly used in graph learning frameworks such as scPGCL.
